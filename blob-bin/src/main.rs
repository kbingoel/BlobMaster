//! blob-bin — CLI entry point. Session 1.1 ships a no-op binary so the
//! workspace builds; Section 6 fills in the `clap` subcommands.

fn main() {
    println!("blobmaster CLI — not yet implemented");
}
